/**
 * Foundation placeholder.
 *
 * Phase 1 establishes the project foundation only. Section components (Hero,
 * About, Projects, Journey, GitHub, Contact, etc.) are built in later phases.
 * This neutral placeholder confirms the app renders and the toolchain works.
 */
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 p-8 text-center">
      <p className="text-muted-foreground font-mono text-sm tracking-widest uppercase">
        Foundation ready
      </p>
      <h1 className="text-2xl font-medium">Gatik Tulsiani</h1>
    </main>
  );
}
